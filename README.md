# Propogate-Kodi-Config
Windows CMD utility to clone Kodi Configs between different fire sticks


## How to Use:

Still in dev, so `.ini` files are not used yet.

For now, run
```
./propogate-Kodi.bat <src firestick IP> <dest firestick IP>
```

